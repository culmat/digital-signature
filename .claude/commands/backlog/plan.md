---
name: "Backlog: Plan"
description: "Create an OpenSpec change for a backlog item"
category: Backlog
tags: [backlog, plan, openspec]
---

Create an OpenSpec change for a backlog item.

Run: `./backlog.sh plan $ARGUMENTS`

Show the output to the user. On success, suggest next steps: use `/opsx:new` or `/opsx:ff` to start working on the change.
